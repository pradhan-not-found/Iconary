name: Create Initial Issues

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/create-issues.yml'

permissions:
  issues: write

jobs:
  create_issues:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create Issues
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |

          gh label create "mentorship-available" || true
          gh label create "hacktoberfest" || true
          gh label create "open source" || true
          gh label create "machine learning" || true
          gh label create "contribution" || true
          gh label create "UI" || true
          gh label create "good first issue" || true
          gh label create "help wanted" || true
          gh label create "hard" || true
          gh label create "optimization" || true
          gh label create "architecture" || true
          gh label create "tooling" || true
          gh label create "testing" || true
          gh label create "up-for-grabs" || true
          gh label create "automation" || true
          gh label create "design" || true
          gh label create "PWA" || true
          gh label create "backend" || true
          gh label create "easy" || true
          gh label create "medium" || true
          gh label create "accessibility" || true
          gh label create "refactoring" || true
          gh label create "community" || true
          gh label create "documentation" || true
          gh label create "icon-request" || true

          # Check if issue already exists to avoid duplicates
          if ! gh issue list --search "in:title Neural Networks" | grep -q "Neural Networks"; then
            gh issue create \
              --title "Add an icon for Neural Networks" \
              --body "We need a new icon representing a Neural Network. 

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "mentorship-available" --label "contribution"
            sleep 2
          fi

          if ! gh issue list --search "in:title Vector Database" | grep -q "Vector Database"; then
            gh issue create \
              --title "Add an icon for Vector Database" \
              --body "We need a new icon representing a Vector Database.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "open source" --label "contribution"
            sleep 2
          fi

          if ! gh issue list --search "in:title Vue.js Support" | grep -q "Vue.js Support"; then
            gh issue create \
              --title "Add Vue.js Support" \
              --body "We currently support React, but we want to make the Iconary library available for Vue.js developers as well!

              ## Requirements
              - Create a new Vue package in the repository.
              - Ensure the build pipeline exports Vue components correctly.
              - Update the README with Vue installation instructions." \
              --label "machine learning" --label "UI"
            sleep 2
          fi

          if ! gh issue list --search "in:title Large Language Model (LLM)" | grep -q "Large Language Model (LLM)"; then
            gh issue create \
              --title "Add an icon for Large Language Model (LLM)" \
              --body "We need a core icon representing a Large Language Model.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "machine learning" --label "UI"
            sleep 2
          fi

          if ! gh issue list --search "in:title RAG Architecture" | grep -q "RAG Architecture"; then
            gh issue create \
              --title "Add an icon for RAG Architecture" \
              --body "We need an icon representing Retrieval-Augmented Generation (RAG).

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "contribution"
            sleep 2
          fi

          if ! gh issue list --search "in:title Authentication Lock" | grep -q "Authentication Lock"; then
            gh issue create \
              --title "Add an icon for Authentication Lock" \
              --body "We need a standard lock icon for authentication and security UI.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "help wanted" --label "machine learning"
            sleep 2
          fi

          if ! gh issue list --search "in:title TailwindCSS" | grep -q "TailwindCSS"; then
            gh issue create \
              --title "Add an icon for TailwindCSS" \
              --body "We need a technology icon representing the TailwindCSS framework.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "good first issue" --label "open source"
            sleep 2
          fi

          if ! gh issue list --search "in:title Figma Community File" | grep -q "Figma Community File"; then
            gh issue create \
              --title "Create a Figma Community File" \
              --body "To make Iconary easier for designers to use, we should publish an official Figma community file.

              ## Requirements
              - Create a Figma file with all current icons.
              - Organize by category.
              - Include variants (Outline, Filled, Duotone) as components." \
              --label "good first issue" --label "UI"
            sleep 2
          fi

          if ! gh issue list --search "in:title Dark Mode Toggle" | grep -q "Dark Mode Toggle"; then
            gh issue create \
              --title "Add Dark Mode Toggle icon" \
              --body "We need a sun/moon hybrid icon or standard toggle for switching to Dark Mode.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "hacktoberfest" --label "machine learning"
            sleep 2
          fi

          if ! gh issue list --search "in:title Next.js" | grep -q "Next.js"; then
            gh issue create \
              --title "Add an icon for Next.js" \
              --body "We need an icon representing the Next.js framework.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "hacktoberfest" --label "open source"
            sleep 2
          fi

          if ! gh issue list --search "in:title Dataset" | grep -q "Dataset"; then
            gh issue create \
              --title "Add an icon for Dataset" \
              --body "We need an icon representing a Dataset used in ML training or evaluation.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "open source" --label "contribution"
            sleep 2
          fi

          if ! gh issue list --search "in:title Prompt Engineering" | grep -q "Prompt Engineering"; then
            gh issue create \
              --title "Add an icon for Prompt Engineering" \
              --body "We need an icon representing the concept of Prompt Engineering for LLMs.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "help wanted" --label "machine learning"
            sleep 2
          fi

          if ! gh issue list --search "in:title GPU Accelerator" | grep -q "GPU Accelerator"; then
            gh issue create \
              --title "Add an icon for GPU Accelerator" \
              --body "We need an icon representing a GPU or hardware accelerator used for AI workloads.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "help wanted" --label "open source"
            sleep 2
          fi

          if ! gh issue list --search "in:title Knowledge Graph" | grep -q "Knowledge Graph"; then
            gh issue create \
              --title "Add an icon for Knowledge Graph" \
              --body "We need an icon representing a Knowledge Graph, often used with RAG architectures.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "help wanted" --label "machine learning"
            sleep 2
          fi

          if ! gh issue list --search "in:title Tokenization" | grep -q "Tokenization"; then
            gh issue create \
              --title "Add an icon for Tokenization" \
              --body "We need an icon representing the concept of Tokenization (splitting text into tokens).

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "good first issue" --label "open source"
            sleep 2
          fi

          if ! gh issue list --search "in:title Generative Adversarial Network" | grep -q "Generative Adversarial Network"; then
            gh issue create \
              --title "Add an icon for Generative Adversarial Network (GAN)" \
              --body "We need an icon representing a GAN architecture.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "good first issue" --label "open source"
            sleep 2
          fi

          if ! gh issue list --search "in:title Reinforcement Learning" | grep -q "Reinforcement Learning"; then
            gh issue create \
              --title "Add an icon for Reinforcement Learning" \
              --body "We need an icon representing Reinforcement Learning (agents, rewards, environments).

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "hacktoberfest" --label "machine learning"
            sleep 2
          fi

          if ! gh issue list --search "in:title Attention Mechanism" | grep -q "Attention Mechanism"; then
            gh issue create \
              --title "Add an icon for Attention Mechanism" \
              --body "We need an icon representing the Attention Mechanism used in Transformers.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "help wanted" --label "open source"
            sleep 2
          fi

          if ! gh issue list --search "in:title Hyperparameter Tuning" | grep -q "Hyperparameter Tuning"; then
            gh issue create \
              --title "Add an icon for Hyperparameter Tuning" \
              --body "We need an icon representing Hyperparameter Tuning or Optimization.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "contribution"
            sleep 2
          fi

          if ! gh issue list --search "in:title Bias and Fairness" | grep -q "Bias and Fairness"; then
            gh issue create \
              --title "Add an icon for Bias and Fairness in AI" \
              --body "We need an icon representing the concept of Bias and Fairness in machine learning models.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "good first issue" --label "open source"
            sleep 2
          fi

          if ! gh issue list --search "in:title Data Pipeline" | grep -q "Data Pipeline"; then
            gh issue create \
              --title "Add an icon for Data Pipeline / ETL" \
              --body "We need an icon representing a Data Pipeline or ETL (Extract, Transform, Load) process.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "open source" --label "contribution"
            sleep 2
          fi

          if ! gh issue list --search "in:title Edge AI" | grep -q "Edge AI"; then
            gh issue create \
              --title "Add an icon for Edge AI" \
              --body "We need an icon representing Edge AI (running models on local or edge devices).

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "hacktoberfest" --label "machine learning"
            sleep 2
          fi

          if ! gh issue list --search "in:title Model Checkpoint" | grep -q "Model Checkpoint"; then
            gh issue create \
              --title "Add an icon for Model Checkpoint" \
              --body "We need an icon representing a Model Checkpoint saved during training.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "good first issue" --label "UI"
            sleep 2
          fi

          if ! gh issue list --search "in:title AI Hallucination" | grep -q "AI Hallucination"; then
            gh issue create \
              --title "Add an icon for AI Hallucination" \
              --body "We need an icon representing an AI Hallucination or an incorrect confident response.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "good first issue" --label "open source"
            sleep 2
          fi

          if ! gh issue list --search "in:title MLOps" | grep -q "MLOps"; then
            gh issue create \
              --title "Add an icon for MLOps" \
              --body "We need an icon representing MLOps (Machine Learning Operations) workflows.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "open source" --label "contribution"
            sleep 2
          fi

          if ! gh issue list --search "in:title Zero-Shot Learning" | grep -q "Zero-Shot Learning"; then
            gh issue create \
              --title "Add an icon for Zero-Shot Learning" \
              --body "We need an icon representing Zero-Shot Learning for AI models.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "good first issue" --label "UI"
            sleep 2
          fi

          if ! gh issue list --search "in:title Transfer Learning" | grep -q "Transfer Learning"; then
            gh issue create \
              --title "Add an icon for Transfer Learning" \
              --body "We need an icon representing the concept of Transfer Learning (transferring weights from one model to another).

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "mentorship-available" --label "contribution"
            sleep 2
          fi

          if ! gh issue list --search "in:title Computer Vision" | grep -q "Computer Vision"; then
            gh issue create \
              --title "Add an icon for Computer Vision" \
              --body "We need an icon representing Computer Vision tasks or CNN architectures.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "good first issue" --label "UI"
            sleep 2
          fi

          if ! gh issue list --search "in:title NLP" | grep -q "NLP"; then
            gh issue create \
              --title "Add an icon for NLP (Natural Language Processing)" \
              --body "We need an icon representing NLP tasks (Natural Language Processing).

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "hacktoberfest" --label "machine learning"
            sleep 2
          fi

          if ! gh issue list --search "in:title Model Weights" | grep -q "Model Weights"; then
            gh issue create \
              --title "Add an icon for Model Weights" \
              --body "We need an icon representing Model Weights or Parameters.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "contribution"
            sleep 2
          fi

          if ! gh issue list --search "in:title Activation Function" | grep -q "Activation Function"; then
            gh issue create \
              --title "Add an icon for Activation Function" \
              --body "We need an icon representing an Activation Function (like ReLU, Sigmoid).

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "mentorship-available" --label "contribution"
            sleep 2
          fi

          if ! gh issue list --search "in:title Gradient Descent" | grep -q "Gradient Descent"; then
            gh issue create \
              --title "Add an icon for Gradient Descent" \
              --body "We need an icon representing the concept of Gradient Descent or loss minimization.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "help wanted" --label "machine learning"
            sleep 2
          fi

          if ! gh issue list --search "in:title Epoch" | grep -q "Epoch"; then
            gh issue create \
              --title "Add an icon for Epoch" \
              --body "We need an icon representing an Epoch (training cycle).

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "open source" --label "contribution"
            sleep 2
          fi

          if ! gh issue list --search "in:title Model Deployment" | grep -q "Model Deployment"; then
            gh issue create \
              --title "Add an icon for Model Deployment" \
              --body "We need an icon representing the deployment of a machine learning model to production.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "hacktoberfest" --label "machine learning" --label "open source"
            sleep 2
          fi

          if ! gh issue list --search "in:title Data Drift" | grep -q "Data Drift"; then
            gh issue create \
              --title "Add an icon for Data Drift" \
              --body "We need an icon representing the concept of Data Drift (when the distribution of data changes over time).

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "machine learning" --label "contribution"
            sleep 2
          fi

          if ! gh issue list --search "in:title Overfitting" | grep -q "Overfitting"; then
            gh issue create \
              --title "Add an icon for Overfitting" \
              --body "We need an icon representing Overfitting in a machine learning model.

              ## Requirements
              - Must have outline, filled, and duotone variants.
              - Follow the style guide in \`STYLE_GUIDE.md\`.
              - Include a 1-2 line concept explainer.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "mentorship-available" --label "open source"
            sleep 2
          fi

          if ! gh issue list --search "in:title Implement WebGL Renderer" | grep -q "Implement WebGL Renderer"; then
            gh issue create \
              --title "Implement WebGL Renderer for Icon Gallery" \
              --body "The current icon gallery can get slow when rendering thousands of SVG nodes. We need a WebGL-based renderer (e.g., using PixiJS or Three.js) to virtualize and render icons at 60fps. This is a complex architecture task.

              See \\`CONTRIBUTING.md\\` to get started!" \
              --label "hard" --label "optimization" --label "architecture"
            sleep 2
          fi

          if ! gh issue list --search "in:title CLI tool for bulk SVG optimization" | grep -q "CLI tool for bulk SVG optimization"; then
            gh issue create \
              --title "Add a CLI tool for bulk SVG optimization" \
              --body "We need a Node.js CLI tool that runs SVGO with custom plugins to optimize all icons in the \\`icons/\\` directory, ensuring consistent stroke widths and removing redundant paths. Must include unit tests.

              See \\`CONTRIBUTING.md\\` to get started!" \
              --label "hard" --label "tooling" --label "hacktoberfest"
            sleep 2
          fi

          if ! gh issue list --search "in:title Implement Figma Plugin" | grep -q "Implement Figma Plugin"; then
            gh issue create \
              --title "Implement Figma Plugin for Iconary" \
              --body "Designers need a way to drag and drop Iconary icons directly into their Figma files. Create a Figma Plugin using their API that fetches icons from our repository and inserts them as vector nodes.

              See \\`CONTRIBUTING.md\\` to get started!" \
              --label "hard" --label "UI" --label "open source"
            sleep 2
          fi

          if ! gh issue list --search "in:title Create a VS Code Extension" | grep -q "Create a VS Code Extension"; then
            gh issue create \
              --title "Create a VS Code Extension for Iconary" \
              --body "Build a VS Code extension that provides autocomplete for Iconary icon names and renders a preview of the icon on hover. This requires deep knowledge of the VS Code Extension API.

              See \\`CONTRIBUTING.md\\` to get started!" \
              --label "hard" --label "tooling" --label "mentorship-available"
            sleep 2
          fi

          if ! gh issue list --search "in:title animated SVG variants" | grep -q "animated SVG variants"; then
            gh issue create \
              --title "Add support for animated SVG variants" \
              --body "We want to introduce a new 'animated' variant for core icons (e.g., a spinning loader, a pulsing heart). Define the animation standards and implement the first 10 animated icons using CSS/SMIL.

              See \\`CONTRIBUTING.md\\` to get started!" \
              --label "hard" --label "UI" --label "architecture"
            sleep 2
          fi

          if ! gh issue list --search "in:title Migrate documentation to Next.js App Router" | grep -q "Migrate documentation to Next.js App Router"; then
            gh issue create \
              --title "Migrate documentation to Next.js App Router" \
              --body "Our current documentation site needs to be migrated to the Next.js App Router to take advantage of React Server Components and improved routing patterns. This is a major refactor.

              See \\`CONTRIBUTING.md\\` to get started!" \
              --label "hard" --label "architecture" --label "hacktoberfest"
            sleep 2
          fi

          if ! gh issue list --search "in:title Implement an Icon Font generator" | grep -q "Implement an Icon Font generator"; then
            gh issue create \
              --title "Implement an Icon Font generator" \
              --body "While SVGs are preferred, many users still want an icon font (.woff2). Implement a build step that automatically generates an icon font and corresponding CSS classes from our SVG assets.

              See \\`CONTRIBUTING.md\\` to get started!" \
              --label "hard" --label "tooling" --label "mentorship-available"
            sleep 2
          fi

          if ! gh issue list --search "in:title robust E2E testing with Playwright" | grep -q "robust E2E testing with Playwright"; then
            gh issue create \
              --title "Add robust E2E testing with Playwright" \
              --body "Set up a full End-to-End testing suite using Playwright for the Iconary web platform. Cover all critical user flows: browsing, searching, modal interactions, and downloading.

              See \\`CONTRIBUTING.md\\` to get started!" \
              --label "hard" --label "testing" --label "open source"
            sleep 2
          fi

          if ! gh issue list --search "in:title semantic search with vector embeddings" | grep -q "semantic search with vector embeddings"; then
            gh issue create \
              --title "Implement semantic search with vector embeddings" \
              --body "Instead of just keyword search, we want semantic search for icons (e.g., searching 'robot' should return the 'AI' icon). Use a lightweight embeddings model and a vector store to power the search.

              See \\`CONTRIBUTING.md\\` to get started!" \
              --label "hard" --label "machine learning" --label "architecture"
            sleep 2
          fi

          if ! gh issue list --search "in:title automated Accessibility" | grep -q "automated Accessibility"; then
            gh issue create \
              --title "Build an automated Accessibility (a11y) linter" \
              --body "Write a custom script that validates all SVG files in the repository to ensure they have proper \\`<title>\\` tags, \\`aria-hidden\\` attributes where appropriate, and pass standard accessibility checks.

              See \\`CONTRIBUTING.md\\` to get started!" \
              --label "hard" --label "tooling" --label "up-for-grabs"
            sleep 2
          fi

          if ! gh issue list --search "in:title Implement SVGR integration" | grep -q "Implement SVGR integration"; then
            gh issue create \
              --title "Implement SVGR integration for React components" \
              --body "Instead of manually building React components, we should use SVGR to automate the transformation of SVGs into React components during our build step. This will make maintaining the React package much easier.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "tooling" --label "UI"
            sleep 2
          fi

          if ! gh issue list --search "in:title automatic release notes" | grep -q "automatic release notes"; then
            gh issue create \
              --title "Add a GitHub Action for automatic release notes" \
              --body "We need a GitHub Action that automatically generates comprehensive release notes based on PR labels (e.g., feature, bugfix, documentation) whenever a new tag is pushed.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "tooling" --label "automation"
            sleep 2
          fi

          if ! gh issue list --search "in:title Monorepo using Turborepo" | grep -q "Monorepo using Turborepo"; then
            gh issue create \
              --title "Migrate Iconary to a Monorepo using Turborepo" \
              --body "As we add more framework packages (Vue, Angular, Svelte), the repository structure needs to be migrated to a proper monorepo using Turborepo for optimized build caching and task orchestration.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "hard" --label "architecture" --label "tooling"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a new logo" | grep -q "Design a new logo"; then
            gh issue create \
              --title "Design a new logo for Iconary" \
              --body "The project needs a distinct and memorable logo. We are looking for something clean, modern, and related to vectors, AI, or geometric primitives. Open to all design contributions!

              See \`CONTRIBUTING.md\` to get started!" \
              --label "design" --label "UI" --label "up-for-grabs"
            sleep 2
          fi

          if ! gh issue list --search "in:title generic <Icon> wrapper with React.Suspense" | grep -q "generic <Icon> wrapper with React.Suspense"; then
            gh issue create \
              --title "Implement a generic <Icon> wrapper with React.Suspense" \
              --body "To improve initial load times, we should build a generic \\`<Icon>\\` wrapper component that lazy-loads specific SVG paths on demand using React.lazy and Suspense.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "hard" --label "architecture" --label "optimization"
            sleep 2
          fi

          if ! gh issue list --search "in:title Algolia integration" | grep -q "Algolia integration"; then
            gh issue create \
              --title "Add a search indexer for Algolia integration" \
              --body "We want to integrate Algolia for lightning-fast search on the docs site. Build a Node.js script that parses all icon metadata and pushes the index to Algolia on every release.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "tooling" --label "backend"
            sleep 2
          fi

          if ! gh issue list --search "in:title offline caching via Service Workers" | grep -q "offline caching via Service Workers"; then
            gh issue create \
              --title "Implement offline caching via Service Workers" \
              --body "The Iconary browse site should work entirely offline. Implement a Service Worker strategy (using Workbox) to cache the application shell and all SVG assets.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "hard" --label "PWA" --label "optimization"
            sleep 2
          fi

          if ! gh issue list --search "in:title Angular adapter" | grep -q "Angular adapter"; then
            gh issue create \
              --title "Create an Angular adapter for the Iconary core" \
              --body "We have packages for React and Vue, but we need native Angular components. Create the build pipeline and component templates to export Angular-compatible icons.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "architecture" --label "contribution"
            sleep 2
          fi

          if ! gh issue list --search "in:title design system to Storybook" | grep -q "design system to Storybook"; then
            gh issue create \
              --title "Publish Iconary's design system to Storybook" \
              --body "Set up Storybook to document our internal UI components (modals, buttons, dropdowns) used on the website. This will make it easier for contributors to build new pages.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "tooling" --label "up-for-grabs"
            sleep 2
          fi

          if ! gh issue list --search "in:title integration tests for SVGO" | grep -q "integration tests for SVGO"; then
            gh issue create \
              --title "Add integration tests for SVGO compression rates" \
              --body "Write a test suite that runs our SVGO pipeline on a set of benchmark icons and fails the CI build if the compression rate drops below an acceptable threshold or if visual regressions occur.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "testing" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title interactive 'Icon Builder' playground" | grep -q "interactive 'Icon Builder' playground"; then
            gh issue create \
              --title "Implement an interactive 'Icon Builder' playground" \
              --body "Create a new page on the web app where users can dynamically mix and match geometric shapes to generate their own custom SVG icons and export them.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "hard" --label "mentorship-available"
            sleep 2
          fi

          if ! gh issue list --search "in:title reduce tailwind.css bundle size" | grep -q "reduce tailwind.css bundle size"; then
            gh issue create \
              --title "Audit and reduce tailwind.css bundle size" \
              --body "Our production CSS bundle is slightly larger than necessary. Audit the Tailwind configuration, remove unused generic utilities, and implement strict purge paths.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "optimization" --label "UI"
            sleep 2
          fi

          if ! gh issue list --search "in:title custom icon bundles CLI" | grep -q "custom icon bundles CLI"; then
            gh issue create \
              --title "Create a CLI to generate custom icon bundles" \
              --body "Provide a CLI tool (e.g. \\`npx create-iconary-bundle\\`) that allows developers to select exactly which icons they want and generates a tiny, optimized file just for those icons.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "tooling" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title agnostic Web Components" | grep -q "agnostic Web Components"; then
            gh issue create \
              --title "Support generic framework-agnostic Web Components" \
              --body "Using Lit or standard web APIs, wrap our SVGs into generic Web Components (\\`<iconary-icon>\\`) so they can be dropped into any tech stack without a specific framework wrapper.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "architecture" --label "open source"
            sleep 2
          fi

          if ! gh issue list --search "in:title internationalization (i18n)" | grep -q "internationalization (i18n)"; then
            gh issue create \
              --title "Add internationalization (i18n) for icon tags and descriptions" \
              --body "To support a global community, we need to translate our icon search tags and concept descriptions into multiple languages (starting with Spanish and French). Set up the i18n infrastructure.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "contribution" --label "UI"
            sleep 2
          fi

          if ! gh issue list --search "in:title Fix typo in README.md" | grep -q "Fix typo in README.md"; then
            gh issue create \
              --title "Fix typo in README.md" \
              --body "There are a few minor grammatical and spelling errors in the README.md. Please review the document and fix them. Great first issue!

              See \`CONTRIBUTING.md\` to get started!" \
              --label "good first issue" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title hover effect to the Browse button" | grep -q "hover effect to the Browse button"; then
            gh issue create \
              --title "Add hover effect to the Browse button" \
              --body "The 'Browse icons' button on the hero section feels a bit static. Let's add a subtle scale-up transform or background color transition on hover to make it feel more interactive.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Improve contrast ratio for placeholder text" | grep -q "Improve contrast ratio for placeholder text"; then
            gh issue create \
              --title "Improve contrast ratio for placeholder text in search bar" \
              --body "The placeholder text in our search bar doesn't currently meet WCAG AA contrast standards. We need to darken the text color slightly to ensure it's readable for everyone.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "accessibility" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title GitHub issue templates" | grep -q "GitHub issue templates"; then
            gh issue create \
              --title "Add GitHub issue templates" \
              --body "Create standard GitHub issue templates (Bug Report, Feature Request, New Icon Request) in the \\`.github/ISSUE_TEMPLATE\\` directory to help contributors provide better context.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "tooling" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Scroll to top button" | grep -q "Scroll to top button"; then
            gh issue create \
              --title "Add a 'Scroll to top' button on the Browse page" \
              --body "When a user scrolls far down the browse page, it takes a while to get back up. Add a small FAB (Floating Action Button) in the bottom right corner that appears after scrolling and smoothly scrolls the user back to the top.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title keyboard navigation for the Icon Modal" | grep -q "keyboard navigation for the Icon Modal"; then
            gh issue create \
              --title "Implement keyboard navigation for the Icon Modal" \
              --body "Users should be able to navigate the Icon Modal entirely with their keyboard. Ensure the 'Copy', 'Download', and dropdown buttons are focusable via 'Tab' and triggerable via 'Enter'/'Space'.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "medium" --label "accessibility"
            sleep 2
          fi

          if ! gh issue list --search "in:title Copy SVG keyboard shortcut" | grep -q "Copy SVG keyboard shortcut"; then
            gh issue create \
              --title "Add a 'Copy SVG' keyboard shortcut" \
              --body "When a user has an icon modal open, pressing \\`Ctrl+C\\` (or \\`Cmd+C\\` on Mac) should automatically copy the raw SVG code to their clipboard.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title auto-label PRs" | grep -q "auto-label PRs"; then
            gh issue create \
              --title "Create a GitHub Action to auto-label PRs" \
              --body "Implement a GitHub action that automatically labels Pull Requests based on the files changed. For example, changes to \\`src/components\\` get a 'React' label, changes to \\`icons/\\` get an 'Icon' label.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "automation" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Toast notification when an icon is copied" | grep -q "Toast notification when an icon is copied"; then
            gh issue create \
              --title "Add a 'Toast' notification when an icon is copied" \
              --body "We currently show 'Copied!' on the button itself. Let's also add a global toast notification (using a library like Sonner) that appears at the bottom of the screen.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title custom hook for search logic" | grep -q "custom hook for search logic"; then
            gh issue create \
              --title "Refactor Browse.tsx to use a custom hook for search logic" \
              --body "The search and filtering logic in \\`Browse.tsx\\` is getting a bit bulky. Extract this logic into a clean, reusable \\`useIconSearch\\` custom hook to separate UI from business logic.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "refactoring" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title fuzzy search support using fuse.js" | grep -q "fuzzy search support using fuse.js"; then
            gh issue create \
              --title "Add fuzzy search support using fuse.js" \
              --body "Our current search is an exact substring match. Integrate \\`fuse.js\\` to allow for fuzzy searching so users can still find icons even if they make slight typos.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "tooling" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title strip data attributes from production SVGs" | grep -q "strip data attributes from production SVGs"; then
            gh issue create \
              --title "Set up a Webpack plugin to strip data-* attributes from production SVGs" \
              --body "We use some internal \\`data-*\\` attributes on our SVGs for testing and internal logic. Write a custom build plugin that safely strips these attributes before final production bundling to save bytes.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "tooling" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title custom SVG parser to validate incoming icon contributions" | grep -q "custom SVG parser to validate incoming icon contributions"; then
            gh issue create \
              --title "Implement a custom SVG parser to validate incoming icon contributions in CI" \
              --body "When users submit new icons via PR, we need an automated CI script that parses the raw SVG string and ensures it strictly follows our style guidelines (e.g. correct viewBox, no inline styles, only allowed tags).

              See \`CONTRIBUTING.md\` to get started!" \
              --label "automation" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title dynamic OG images" | grep -q "dynamic OG images"; then
            gh issue create \
              --title "Create a Next.js API route to generate dynamic OG images for each icon" \
              --body "When a specific icon is shared on social media, we want a dynamic Open Graph image generated on the fly that displays the icon on a beautiful branded background. (Requires setting up an Edge Function using @vercel/og).

              See \`CONTRIBUTING.md\` to get started!" \
              --label "backend" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title rust-based WASM module" | grep -q "rust-based WASM module"; then
            gh issue create \
              --title "Add a Rust-based WASM module for ultra-fast client-side SVG optimization" \
              --body "SVGO running in the browser can be slow for massive SVGs. Build a Rust library that performs AST-level SVG optimization, compile it to WebAssembly, and integrate it into our web app for real-time processing.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "architecture" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Copy as React Component" | grep -q "Copy as React Component"; then
            gh issue create \
              --title "Add a 'Copy as React Component' button to the Icon Modal" \
              --body "Currently, users can only copy the raw SVG. Let's add a button to the modal that copies the icon as a fully functional React component (using JSX syntax).

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Open in CodeSandbox" | grep -q "Open in CodeSandbox"; then
            gh issue create \
              --title "Add an 'Open in CodeSandbox' button for icons" \
              --body "It would be amazing if users could instantly test an icon. Add a button that takes the SVG and opens a pre-configured CodeSandbox React environment with the icon rendered.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "medium" --label "tooling"
            sleep 2
          fi

          if ! gh issue list --search "in:title Download All button as a ZIP" | grep -q "Download All button as a ZIP"; then
            gh issue create \
              --title "Add a 'Download All' button to export all icons as a ZIP" \
              --body "Designers often want the whole library at once. Implement a client-side ZIP generator (using something like JSZip) that bundles all SVGs and downloads them.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "tooling" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title deploy Storybook to GitHub Pages" | grep -q "deploy Storybook to GitHub Pages"; then
            gh issue create \
              --title "Build a GitHub Action to deploy Storybook to GitHub Pages" \
              --body "We need our UI components documented. Create a GitHub workflow that builds Storybook and deploys it to the \`gh-pages\` branch automatically on pushes to \`main\`.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "automation" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title unit tests for the IconModal" | grep -q "unit tests for the IconModal"; then
            gh issue create \
              --title "Add unit tests for the <IconModal> component using Vitest" \
              --body "The IconModal is a complex component. We need comprehensive unit tests using Vitest and React Testing Library to ensure it renders correctly and handles state changes.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "testing" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title CLI to scaffold new icons" | grep -q "CLI to scaffold new icons"; then
            gh issue create \
              --title "Implement an 'Iconary' CLI to scaffold new icons locally" \
              --body "Create a Node.js CLI script (e.g., \`npm run create-icon\`) that prompts the contributor for an icon name, creates the boilerplate SVG file, and updates the necessary registry files automatically.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "tooling" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Related Icons section" | grep -q "Related Icons section"; then
            gh issue create \
              --title "Add a 'Related Icons' section to the Icon Modal" \
              --body "When viewing an icon, it would be useful to see 3-4 'related' icons at the bottom of the modal. This requires implementing a lightweight similarity algorithm based on tags or categories.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "architecture" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title dark mode across the entire docs site" | grep -q "dark mode across the entire docs site"; then
            gh issue create \
              --title "Implement dark mode across the entire docs site" \
              --body "The current docs site is hardcoded to a specific theme. Implement a fully robust dark/light mode toggle that respects system preferences and persists in localStorage.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Random Icon button" | grep -q "Random Icon button"; then
            gh issue create \
              --title "Add a 'Random Icon' button to discover new icons" \
              --body "Add a fun little 'dice' button near the search bar that selects a random icon from the library and opens its modal for serendipitous discovery.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Optimize the Vite build" | grep -q "Optimize the Vite build"; then
            gh issue create \
              --title "Optimize the Vite build to split chunks more efficiently" \
              --body "Our production bundle is getting a bit large. Tweak the Vite rollupOptions to intelligently split vendor chunks (like React and Hugeicons) from our application code.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "optimization" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Lighthouse 100/100 scores" | grep -q "Lighthouse 100/100 scores"; then
            gh issue create \
              --title "Audit website for Lighthouse 100/100 scores" \
              --body "Run Google Lighthouse on the production site, identify any performance, accessibility, or SEO bottlenecks, and submit a PR to fix them so we hit perfect 100s across the board.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "optimization" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Contribute Icon form" | grep -q "Contribute Icon form"; then
            gh issue create \
              --title "Implement a 'Contribute Icon' form directly on the website" \
              --body "Instead of forcing contributors to use Git, build a web form where they can paste their SVG code, fill out the metadata, and have it automatically open a Pull Request via the GitHub API.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "hard" --label "automation"
            sleep 2
          fi

          if ! gh issue list --search "in:title Getting Started video tutorial" | grep -q "Getting Started video tutorial"; then
            gh issue create \
              --title "Create a 'Getting Started' video tutorial for contributors" \
              --body "Record a short 3-5 minute screen share explaining how to clone the repo, run the dev server, and add a new icon. Upload it to YouTube or embed it in the CONTRIBUTING.md.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "documentation" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title View on GitHub button" | grep -q "View on GitHub button"; then
            gh issue create \
              --title "Add a 'View on GitHub' button to the Icon Modal" \
              --body "Add a small link/button inside the Icon Modal that takes the user directly to the source SVG file for that specific icon on GitHub.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Set up Dependabot" | grep -q "Set up Dependabot"; then
            gh issue create \
              --title "Set up Dependabot for automatic dependency updates" \
              --body "Add a \\`.github/dependabot.yml\\` configuration file to automatically check for and open PRs for npm package updates on a weekly basis.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "automation" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title custom 404 page" | grep -q "custom 404 page"; then
            gh issue create \
              --title "Implement a custom 404 page" \
              --body "Our current 404 page is just the default browser or host page. Build a branded, aesthetically pleasing 404 page for the React app.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "UI" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Plausible analytics" | grep -q "Plausible analytics"; then
            gh issue create \
              --title "Add Plausible analytics to the docs site" \
              --body "Integrate a lightweight, privacy-friendly analytics script (like Plausible) into the index.html so we can track page views without compromising user privacy.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "tooling" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Trending Icons section" | grep -q "Trending Icons section"; then
            gh issue create \
              --title "Implement a 'Trending Icons' section on the homepage" \
              --body "We need a way to track which icons are copied/downloaded the most and display the top 5 'Trending Icons' on the homepage. Requires setting up a lightweight database (like Supabase or Redis) and an API route.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "backend" --label "architecture" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title downloading icons in PDF format" | grep -q "downloading icons in PDF format"; then
            gh issue create \
              --title "Add support for downloading icons in PDF format" \
              --body "Some print designers prefer PDF vectors over SVGs. Implement a client-side converter (using pdfkit or similar) to allow downloading the icon as a PDF from the modal.

              See \`CONTRIBUTING.md\` to get started!" \
              --label "tooling" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Transformer Model' Icon" | grep -q "Design a 'Transformer Model' Icon"; then
            gh issue create \
              --title "Design a 'Transformer Model' Icon" \
              --body "We need an icon that represents a Transformer architecture (e.g. self-attention mechanism, multi-headed network). This should be a clean, geometric SVG following our 24x24 grid.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "good first issue"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'GPU / TPU' Icon" | grep -q "Design a 'GPU / TPU' Icon"; then
            gh issue create \
              --title "Design a 'GPU / TPU' Icon" \
              --body "AI requires massive compute. We need a 'GPU' or 'TPU' hardware icon to represent processing power. 

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "good first issue"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design an 'API Endpoint' Icon" | grep -q "Design an 'API Endpoint' Icon"; then
            gh issue create \
              --title "Design an 'API Endpoint' Icon" \
              --body "We need an icon representing an API endpoint or API integration (common for connecting LLM services). 

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Data Pipeline' Icon" | grep -q "Design a 'Data Pipeline' Icon"; then
            gh issue create \
              --title "Design a 'Data Pipeline' Icon" \
              --body "Create an icon that visually represents data flowing through a pipeline (ETL process, data streams).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "good first issue"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Knowledge Graph' Icon" | grep -q "Design a 'Knowledge Graph' Icon"; then
            gh issue create \
              --title "Design a 'Knowledge Graph' Icon" \
              --body "Design an icon for Knowledge Graphs (nodes and connected edges) often used in semantic AI architectures.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Computer Vision' Icon" | grep -q "Design a 'Computer Vision' Icon"; then
            gh issue create \
              --title "Design a 'Computer Vision' Icon" \
              --body "We need an icon for Computer Vision/Image Recognition (perhaps an eye combined with a bounding box or scanner).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "good first issue"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Natural Language Processing' Icon" | grep -q "Design a 'Natural Language Processing' Icon"; then
            gh issue create \
              --title "Design a 'Natural Language Processing' Icon" \
              --body "Create an icon for NLP (Natural Language Processing). A speech bubble combined with waveforms or text nodes might work well.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Decision Tree' Icon" | grep -q "Design a 'Decision Tree' Icon"; then
            gh issue create \
              --title "Design a 'Decision Tree' Icon" \
              --body "We need an icon for a Decision Tree or Random Forest machine learning algorithm.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Model Training' Icon" | grep -q "Design a 'Model Training' Icon"; then
            gh issue create \
              --title "Design a 'Model Training' Icon" \
              --body "Design an icon that represents the process of training an AI model (e.g., a barbell, a brain with a loading symbol, or iterative loops).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "good first issue"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design an 'Overfitting' Icon" | grep -q "Design an 'Overfitting' Icon"; then
            gh issue create \
              --title "Design an 'Overfitting' Icon" \
              --body "We need a conceptual icon for 'Overfitting' in machine learning (a model fitting data too closely, e.g. a jagged line perfectly hitting points).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Data Cleaning' Icon" | grep -q "Design a 'Data Cleaning' Icon"; then
            gh issue create \
              --title "Design a 'Data Cleaning' Icon" \
              --body "Design an icon for Data Cleaning / Scrubbing. A combination of a database/table and a broom or sparkles is a common metaphor.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "good first issue"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Jupyter Notebook' Icon" | grep -q "Design a 'Jupyter Notebook' Icon"; then
            gh issue create \
              --title "Design a 'Jupyter Notebook' Icon" \
              --body "Create a generic icon representing a code/data notebook environment (similar to Jupyter Notebooks).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design an 'Agentic Loop' Icon" | grep -q "Design an 'Agentic Loop' Icon"; then
            gh issue create \
              --title "Design an 'Agentic Loop / Autonomous Loop' Icon" \
              --body "We need a highly specific icon for autonomous AI agents. An 'Agentic Loop' represents the Observe-Reason-Act cycle (like OODA loops). This doesn't exist in standard icon sets!

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Function Calling' Icon" | grep -q "Design a 'Function Calling' Icon"; then
            gh issue create \
              --title "Design a 'Function Calling / Tool Use' Icon" \
              --body "LLMs use tools (Function Calling) to interact with the outside world. We need an icon that represents an AI model reaching out to use a tool (maybe a robot arm with a wrench, or an API socket).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'System Prompt' Icon" | grep -q "Design a 'System Prompt' Icon"; then
            gh issue create \
              --title "Design a 'System Prompt / Metaprompt' Icon" \
              --body "A System Prompt is the foundational instruction set for an LLM. We need an icon that distinguishes a 'System Prompt' from a regular user chat message (perhaps a terminal prompt with a crown or gear).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Context Window' Icon" | grep -q "Design a 'Context Window' Icon"; then
            gh issue create \
              --title "Design a 'Context Window' Icon" \
              --body "We need an icon to represent an LLM's 'Context Window' or 'Context Limit'. This could be a document expanding, a memory buffer, or a framed boundary.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Multi-Agent System' Icon" | grep -q "Design a 'Multi-Agent System' Icon"; then
            gh issue create \
              --title "Design a 'Multi-Agent System' Icon" \
              --body "Represent a Swarm or Multi-Agent System where several AI agents communicate and collaborate. (e.g., connected robot nodes).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Chain of Thought' Icon" | grep -q "Design a 'Chain of Thought' Icon"; then
            gh issue create \
              --title "Design a 'Chain of Thought / Reasoning' Icon" \
              --body "We need an icon for Chain-of-Thought (CoT) reasoning. This represents the step-by-step logic an LLM takes before answering. (e.g., connected thought bubbles or a logical sequence).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Model Hallucination' Icon" | grep -q "Design a 'Model Hallucination' Icon"; then
            gh issue create \
              --title "Design a 'Model Hallucination' Icon" \
              --body "A very rare icon request! We need an icon to represent AI 'Hallucinations' (when a model confidently states false information). Maybe a distorted robot or a mirage/glitch effect.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Tokenization' Icon" | grep -q "Design a 'Tokenization' Icon"; then
            gh issue create \
              --title "Design a 'Tokenization / Token Limit' Icon" \
              --body "Design an icon that represents text being broken down into LLM tokens (e.g., a word being sliced into geometric blocks).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Zero-Shot Prompting' Icon" | grep -q "Design a 'Zero-Shot Prompting' Icon"; then
            gh issue create \
              --title "Design a 'Zero-Shot Prompting' Icon" \
              --body "Design an icon for Zero-Shot prompting (asking a model to do a task without giving it any examples). 

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design an 'Agentic Sandbox' Icon" | grep -q "Design an 'Agentic Sandbox' Icon"; then
            gh issue create \
              --title "Design an 'Agentic Sandbox' Icon" \
              --body "AI coding agents (like Devin or Antigravity) run code in secure Sandboxes. We need an icon representing a secure execution environment or isolated terminal sandbox.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design an 'AI Self-Reflection' Icon" | grep -q "Design an 'AI Self-Reflection' Icon"; then
            gh issue create \
              --title "Design an 'AI Self-Reflection / Eval' Icon" \
              --body "Advanced agents use self-reflection to evaluate their own output and correct mistakes. We need an icon for an AI reviewing or looking at itself (a robot looking in a mirror?).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'LoRA / Fine-Tuning' Icon" | grep -q "Design a 'LoRA / Fine-Tuning' Icon"; then
            gh issue create \
              --title "Design a 'LoRA / Low-Rank Adaptation' Icon" \
              --body "Fine-tuning models using LoRA (Low-Rank Adaptation) is huge right now. We need an icon representing attaching a small, lightweight adapter to a massive foundational model.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Vector Embedding Space' Icon" | grep -q "Design a 'Vector Embedding Space' Icon"; then
            gh issue create \
              --title "Design a 'Vector Embedding Space' Icon" \
              --body "Design an icon visualizing a high-dimensional vector space (often used in RAG and semantic search). Think 3D scatter plots or constellation meshes.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Neural Network Node' Icon" | grep -q "Design a 'Neural Network Node' Icon"; then
            gh issue create \
              --title "Design a 'Neural Network Node' Icon" \
              --body "We need an icon specifically representing a single node/perceptron within a neural network. Maybe a glowing circle with input/output dendrite lines.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Backpropagation' Icon" | grep -q "Design a 'Backpropagation' Icon"; then
            gh issue create \
              --title "Design a 'Backpropagation' Icon" \
              --body "Design an icon for Backpropagation (the backwards calculation of errors in a neural network). Arrows flowing backwards through layers would be ideal.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design an 'Epoch' Icon" | grep -q "Design an 'Epoch' Icon"; then
            gh issue create \
              --title "Design an 'Epoch / Training Cycle' Icon" \
              --body "An Epoch is one complete pass through the training data. We need an icon representing a full data cycle or loop.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Dimensionality Reduction' Icon" | grep -q "Design a 'Dimensionality Reduction' Icon"; then
            gh issue create \
              --title "Design a 'Dimensionality Reduction' Icon" \
              --body "Represent concepts like PCA or t-SNE where high-dimensional data is reduced to 2D/3D. Perhaps a 3D cube casting a 2D shadow.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Synthetic Data' Icon" | grep -q "Design a 'Synthetic Data' Icon"; then
            gh issue create \
              --title "Design a 'Synthetic Data' Icon" \
              --body "Synthetic data is artificially generated data used to train models. We need an icon representing 'fake' or 'manufactured' data (e.g. a database with a wand/sparkles).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Data Lake' Icon" | grep -q "Design a 'Data Lake' Icon"; then
            gh issue create \
              --title "Design a 'Data Lake' Icon" \
              --body "Design an icon for a Data Lake (a massive pool of raw, unstructured data).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Gradient Descent' Icon" | grep -q "Design a 'Gradient Descent' Icon"; then
            gh issue create \
              --title "Design a 'Gradient Descent' Icon" \
              --body "Gradient descent is an optimization algorithm. The classic visualization is a curve/valley with a point stepping down towards the global minimum.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Neural Processing Unit' Icon" | grep -q "Design a 'Neural Processing Unit' Icon"; then
            gh issue create \
              --title "Design a 'Neural Processing Unit (NPU)' Icon" \
              --body "We need an icon specifically for NPUs (AI chips built into modern devices). Needs to look distinct from a standard CPU/GPU.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Prompt Engineering' Icon" | grep -q "Design a 'Prompt Engineering' Icon"; then
            gh issue create \
              --title "Design a 'Prompt Engineering' Icon" \
              --body "Design an icon representing the craft of Prompt Engineering (e.g., tweaking inputs to get the best outputs from an LLM).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Retrieval Augmented Generation' Icon" | grep -q "Design a 'Retrieval Augmented Generation' Icon"; then
            gh issue create \
              --title "Design a 'Retrieval Augmented Generation (RAG)' Icon" \
              --body "RAG combines LLMs with external knowledge bases. We need an icon that shows a model 'fetching' or 'retrieving' data from a document or database before answering.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design an 'Edge AI' Icon" | grep -q "Design an 'Edge AI' Icon"; then
            gh issue create \
              --title "Design an 'Edge AI' Icon" \
              --body "Edge AI means running models locally on mobile devices or IoT sensors rather than the cloud. We need an icon for 'on-device intelligence'.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Generative Adversarial Network' Icon" | grep -q "Design a 'Generative Adversarial Network' Icon"; then
            gh issue create \
              --title "Design a 'Generative Adversarial Network (GAN)' Icon" \
              --body "GANs pit two neural networks against each other (a generator and a discriminator). Design an icon representing two AI entities facing off or verifying each other.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Tensor' Icon" | grep -q "Design a 'Tensor' Icon"; then
            gh issue create \
              --title "Design a 'Tensor' Icon" \
              --body "Tensors are multi-dimensional arrays (the core data structure in Deep Learning). We need an icon representing a 3D matrix or hypercube.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design an 'Activation Function' Icon" | grep -q "Design an 'Activation Function' Icon"; then
            gh issue create \
              --title "Design an 'Activation Function' Icon" \
              --body "Design an icon for Activation Functions (like ReLU, Sigmoid, Tanh). A classic 'S' curve or step function on an axis works perfectly.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Loss Function' Icon" | grep -q "Design a 'Loss Function' Icon"; then
            gh issue create \
              --title "Design a 'Loss Function' Icon" \
              --body "Design an icon for Cost/Loss Functions in Machine Learning. Something representing error calculation or minimizing a gap.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design an 'Attention Mechanism' Icon" | grep -q "Design an 'Attention Mechanism' Icon"; then
            gh issue create \
              --title "Design an 'Attention Mechanism' Icon" \
              --body "The core of modern LLMs! We need an icon representing 'Attention' (focusing on specific parts of a sequence). Maybe a spotlight on specific data nodes.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Reinforcement Learning' Icon" | grep -q "Design a 'Reinforcement Learning' Icon"; then
            gh issue create \
              --title "Design a 'Reinforcement Learning' Icon" \
              --body "Design an icon for RL (Reinforcement Learning). Needs to represent an agent taking an action and receiving a reward (like a plus sign or trophy).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Transfer Learning' Icon" | grep -q "Design a 'Transfer Learning' Icon"; then
            gh issue create \
              --title "Design a 'Transfer Learning' Icon" \
              --body "Transfer learning involves taking knowledge from one model and applying it to another. Design an icon showing 'knowledge transfer' between two brains or networks.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Hyperparameter Tuning' Icon" | grep -q "Design a 'Hyperparameter Tuning' Icon"; then
            gh issue create \
              --title "Design a 'Hyperparameter Tuning' Icon" \
              --body "Design an icon representing Hyperparameter Optimization. Sliders, dials, or tuning knobs on a model architecture are common metaphors.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Cross-Validation' Icon" | grep -q "Design a 'Cross-Validation' Icon"; then
            gh issue create \
              --title "Design a 'Cross-Validation' Icon" \
              --body "Design an icon for K-Fold Cross-Validation (splitting a dataset into chunks/folds to test a model).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design an 'Object Detection' Icon" | grep -q "Design an 'Object Detection' Icon"; then
            gh issue create \
              --title "Design an 'Object Detection' Icon" \
              --body "Design an icon for Object Detection in Computer Vision. Bounding boxes identifying distinct subjects in a frame is standard.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Time Series Analysis' Icon" | grep -q "Design a 'Time Series Analysis' Icon"; then
            gh issue create \
              --title "Design a 'Time Series Analysis' Icon" \
              --body "Design an icon for Time Series Data (data points indexed over time, like stock prices or sensor readings). A line chart combined with a clock/timeline.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Word Embedding' Icon" | grep -q "Design a 'Word Embedding' Icon"; then
            gh issue create \
              --title "Design a 'Word Embedding' Icon" \
              --body "Design an icon representing mapping text/words to mathematical vectors.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design an 'Autoencoder' Icon" | grep -q "Design an 'Autoencoder' Icon"; then
            gh issue create \
              --title "Design an 'Autoencoder' Icon" \
              --body "Autoencoders compress and decompress data. Design an icon showing an 'hourglass' shaped neural network structure.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Diffusion Model' Icon" | grep -q "Design a 'Diffusion Model' Icon"; then
            gh issue create \
              --title "Design a 'Diffusion Model' Icon" \
              --body "Diffusion Models (like Midjourney/Stable Diffusion) work by adding and removing noise from images. Design an icon representing noise transforming into a clear image.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Knowledge Distillation' Icon" | grep -q "Design a 'Knowledge Distillation' Icon"; then
            gh issue create \
              --title "Design a 'Knowledge Distillation' Icon" \
              --body "Knowledge Distillation is compressing a large 'teacher' model into a smaller 'student' model. Design an icon showing a large network transferring data to a smaller one.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Few-Shot Prompting' Icon" | grep -q "Design a 'Few-Shot Prompting' Icon"; then
            gh issue create \
              --title "Design a 'Few-Shot Prompting' Icon" \
              --body "Design an icon for Few-Shot Prompting (providing a model with a small number of examples before asking it to perform a task).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Feature Extraction' Icon" | grep -q "Design a 'Feature Extraction' Icon"; then
            gh issue create \
              --title "Design a 'Feature Extraction' Icon" \
              --body "Design an icon representing Feature Extraction (pulling useful features out of raw data, like extracting edges from an image).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design an 'Outlier Detection' Icon" | grep -q "Design an 'Outlier Detection' Icon"; then
            gh issue create \
              --title "Design an 'Outlier Detection / Anomaly' Icon" \
              --body "We need an icon for detecting anomalies or outliers in a dataset. A scatterplot with one glowing/highlighted point outside the main cluster.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Recommendation System' Icon" | grep -q "Design a 'Recommendation System' Icon"; then
            gh issue create \
              --title "Design a 'Recommendation System' Icon" \
              --body "Design an icon representing an AI Recommendation Engine (matching a user with personalized content, like movies or products).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Graph Neural Network' Icon" | grep -q "Design a 'Graph Neural Network' Icon"; then
            gh issue create \
              --title "Design a 'Graph Neural Network (GNN)' Icon" \
              --body "GNNs process graph-structured data. We need an icon representing a neural network overlaid on a complex node-edge graph.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Federated Learning' Icon" | grep -q "Design a 'Federated Learning' Icon"; then
            gh issue create \
              --title "Design a 'Federated Learning' Icon" \
              --body "Federated Learning involves training a model across multiple decentralized edge devices without exchanging raw data. Design an icon showing edge devices feeding into a central model.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design an 'Explainable AI' Icon" | grep -q "Design an 'Explainable AI' Icon"; then
            gh issue create \
              --title "Design an 'Explainable AI (XAI)' Icon" \
              --body "XAI is about looking inside the 'black box' of neural networks. Design an icon representing transparency (like a magnifying glass over a brain/network).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Swarm Intelligence' Icon" | grep -q "Design a 'Swarm Intelligence' Icon"; then
            gh issue create \
              --title "Design a 'Swarm Intelligence' Icon" \
              --body "Design an icon for Swarm Intelligence (AI systems inspired by the collective behavior of ants, bees, or birds).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "hard"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Computer Vision Segmentation' Icon" | grep -q "Design a 'Computer Vision Segmentation' Icon"; then
            gh issue create \
              --title "Design a 'Computer Vision Segmentation' Icon" \
              --body "Image segmentation involves pixel-perfect boundary tracing around objects. Design an icon showing a precise lasso or mask over an object.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Batch Processing' Icon" | grep -q "Design a 'Batch Processing' Icon"; then
            gh issue create \
              --title "Design a 'Batch Processing' Icon" \
              --body "Design an icon for Batch Processing (processing data in distinct groups rather than continuously).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design a 'Supervised Learning' Icon" | grep -q "Design a 'Supervised Learning' Icon"; then
            gh issue create \
              --title "Design a 'Supervised Learning' Icon" \
              --body "Supervised learning uses labeled data. Design an icon representing a 'teacher' or 'labels' guiding the algorithm.

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Design an 'Unsupervised Learning' Icon" | grep -q "Design an 'Unsupervised Learning' Icon"; then
            gh issue create \
              --title "Design an 'Unsupervised Learning' Icon" \
              --body "Unsupervised learning finds hidden patterns without labels. Design an icon representing discovering structure in chaos (like clustering unorganized dots).

              Check out \\`STYLE_GUIDE.md\\` before you start designing!" \
              --label "icon-request" --label "design" --label "medium"
            sleep 2
          fi

          if ! gh issue list --search "in:title Mixture of Experts" | grep -q "Mixture of Experts"; then
            gh issue create \
              --title "Icon Request: Mixture of Experts (MoE)" \
              --body "**Concept:** Mixture of Experts (MoE)

              **Description:** An AI architecture where multiple specialized sub-models (experts) are dynamically selected to process specific parts of the input, vastly improving efficiency and scale.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title Large Action Model" | grep -q "Large Action Model"; then
            gh issue create \
              --title "Icon Request: Large Action Model (LAM)" \
              --body "**Concept:** Large Action Model (LAM)

              **Description:** AI models designed not just to process text or images, but to execute actions, use interfaces, and interact with software environments autonomously.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title Multi-Agent Framework" | grep -q "Multi-Agent Framework"; then
            gh issue create \
              --title "Icon Request: Multi-Agent Framework" \
              --body "**Concept:** Multi-Agent Framework

              **Description:** Systems where multiple autonomous AI agents collaborate, debate, or divide tasks to achieve complex goals.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title Prompt Injection" | grep -q "Prompt Injection"; then
            gh issue create \
              --title "Icon Request: Prompt Injection / Jailbreak" \
              --body "**Concept:** Prompt Injection / Jailbreak

              **Description:** A cybersecurity threat in AI where malicious inputs are designed to manipulate the model into ignoring safety guardrails or leaking data.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title Model Quantization" | grep -q "Model Quantization"; then
            gh issue create \
              --title "Icon Request: Model Quantization" \
              --body "**Concept:** Model Quantization

              **Description:** The process of reducing the precision of a model's weights (e.g., from 16-bit to 4-bit) to drastically lower memory usage and speed up inference without heavily sacrificing performance.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title KV Cache" | grep -q "KV Cache"; then
            gh issue create \
              --title "Icon Request: KV Cache" \
              --body "**Concept:** Key-Value Cache (KV Cache)

              **Description:** A memory optimization technique used during transformer inference that stores past key and value tensors to prevent redundant recalculations during text generation.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title RLHF" | grep -q "RLHF"; then
            gh issue create \
              --title "Icon Request: RLHF (Reinforcement Learning from Human Feedback)" \
              --body "**Concept:** RLHF

              **Description:** A training method that incorporates human evaluations and preferences into the reward loop to align an AI model's output with human values.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title Direct Preference Optimization" | grep -q "Direct Preference Optimization"; then
            gh issue create \
              --title "Icon Request: DPO (Direct Preference Optimization)" \
              --body "**Concept:** Direct Preference Optimization (DPO)

              **Description:** A more efficient alternative to RLHF that optimizes AI models directly on human preference data without needing a separate reward model.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title Vision-Language Model" | grep -q "Vision-Language Model"; then
            gh issue create \
              --title "Icon Request: Vision-Language Model (VLM)" \
              --body "**Concept:** Vision-Language Model (VLM)

              **Description:** Multimodal AI models capable of simultaneously understanding and generating both visual and textual information.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title Context Window" | grep -q "Context Window"; then
            gh issue create \
              --title "Icon Request: Context Window" \
              --body "**Concept:** Context Window

              **Description:** The maximum amount of text or tokens an AI model can 'remember' and process at one time during a single interaction.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title State Space Model" | grep -q "State Space Model"; then
            gh issue create \
              --title "Icon Request: State Space Model (SSM / Mamba)" \
              --body "**Concept:** State Space Model (SSM)

              **Description:** An emerging alternative architecture to transformers (like Mamba) that scales linearly with sequence length, making it highly efficient for infinite context.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title Model Weights" | grep -q "Model Weights"; then
            gh issue create \
              --title "Icon Request: Model Weights / Safetensors" \
              --body "**Concept:** Model Weights / Safetensors

              **Description:** The massive, serialized files (often stored as Safetensors) containing the trained parameters that physically define the capabilities of the model.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title AI Alignment" | grep -q "AI Alignment"; then
            gh issue create \
              --title "Icon Request: AI Alignment & Safety" \
              --body "**Concept:** AI Alignment & Safety

              **Description:** The field of research ensuring AI models are robust, safe, and their objectives are aligned with human intent.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title Speculative Decoding" | grep -q "Speculative Decoding"; then
            gh issue create \
              --title "Icon Request: Speculative Decoding" \
              --body "**Concept:** Speculative Decoding

              **Description:** An inference optimization where a smaller, faster 'draft' model predicts the next few tokens, and a larger 'target' model verifies them simultaneously, drastically speeding up generation.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title Constitutional AI" | grep -q "Constitutional AI"; then
            gh issue create \
              --title "Icon Request: Constitutional AI" \
              --body "**Concept:** Constitutional AI

              **Description:** A methodology where an AI is trained to self-correct its behavior and critique its own outputs based on a predefined set of rules or a 'constitution'.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "help wanted"
            sleep 2
          fi

          if ! gh issue list --search "in:title GitHub Actions" | grep -q "GitHub Actions"; then
            gh issue create \
              --title "Icon Request: GitHub Actions" \
              --body "**Concept:** GitHub Actions

              **Description:** The standard logo/icon for GitHub Actions, representing CI/CD workflows and automation.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Pull Request" | grep -q "Pull Request"; then
            gh issue create \
              --title "Icon Request: Pull Request" \
              --body "**Concept:** Pull Request

              **Description:** The classic branching and merging symbol that represents a Git Pull Request.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Fork Repository" | grep -q "Fork Repository"; then
            gh issue create \
              --title "Icon Request: Fork Repository" \
              --body "**Concept:** Fork Repository

              **Description:** The standard branching symbol that represents forking a repository.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Merge Commit" | grep -q "Merge Commit"; then
            gh issue create \
              --title "Icon Request: Merge Commit" \
              --body "**Concept:** Merge Commit

              **Description:** A Git symbol indicating two branches converging into a single commit.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title Code Repository" | grep -q "Code Repository"; then
            gh issue create \
              --title "Icon Request: Code Repository" \
              --body "**Concept:** Code Repository

              **Description:** A book or database symbol with a badge or git icon, representing a code repository.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "easy"
            sleep 2
          fi

          if ! gh issue list --search "in:title User Profile" | grep -q "User Profile"; then
            gh issue create \
              --title "Icon Request: User Profile" \
              --body "**Concept:** User Profile

              **Description:** A simple, clean avatar or user icon representing a developer profile.

              **Requirements:**
              - Must have Outline, Filled, and Duotone variants
              - Follow the Iconary style guide

              *This is a great issue for first-time contributors!*" \
              --label "icon-request" --label "good first issue" --label "easy"
            sleep 2
          fi
